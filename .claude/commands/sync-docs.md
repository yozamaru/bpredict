---
description: 実装と設計文書のずれを検出する
---

実装と設計文書のずれを検出してください。

1. `docs/design-detail.md` のスキーマ定義と、`db/migrations/` の実体を突き合わせる
2. API 仕様（3章）と `api/src/routes/` の実装を突き合わせる
3. 特徴量定義（2.2）と `batch/features/` の実装を突き合わせる
4. 画面一覧と `web/app/` のルートを突き合わせる
5. ずれを一覧で報告する。どちらが正しいかは**私に確認する**

実装が正しい場合は文書を更新し、文書が正しい場合は実装の修正案を提示してください。勝手にどちらかへ寄せないこと。
