# 機密 — public リポジトリのため絶対にコミットしない
.env
.env.local
.dev.vars
*.pem
verification/07_d1_batch_query_count/wrangler.toml

# 取得したレスポンス本文（著作権上コミット不可。fixture は合成データのみ）
# ボックススコアは <script> 内の埋め込みJSONで配信されるため、JSON も対象にする
batch/cache/
batch/.scraper-state/
data/raw/
**/fixtures/**/*.html
**/fixtures/**/*.raw.json
*.html.gz

# データ・モデル
*.db
*.sqlite
models/*.pkl
models/*.txt

# 依存・ビルド
node_modules/

# wrangler types の生成物。.dev.vars を読むため環境依存になる（コミットしない）
api/worker-configuration.d.ts
.next/
out/
# `wrangler pages` が Next.js を検出すると `opennextjs-cloudflare build` を走らせて
# これを作る。**この構成は採用していない**（ISR が R2/KV を要求し、絶対ルール5に
# 触れる。要件 7章）。残すと ESLint が生成物を lint して落ちる
.open-next/
__pycache__/
*.pyc
.venv/
venv/
.wrangler/

# OS・エディタ
.DS_Store
.vscode/
.idea/
pytest.log
