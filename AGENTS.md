# エージェント共通の入口

このリポジトリは、複数の AI エージェント（Claude Code / Codex など）が**交代で**作業する。
利用制限で片方が止まったら、もう片方が続きを引き取る。

## 最初に読むもの

1. **[CLAUDE.md](CLAUDE.md)** — **絶対ルール、設計文書の優先順位、Git の運用、止まるべき条件。
   エージェントの別を問わず、ここが唯一の規約である。**
2. **[docs/STATUS.md](docs/STATUS.md)** — いまどこまで進んでいて、次に何をするか。
   判断待ちの項目と、リポジトリからは分からない本番の状態が書いてある
3. [docs/README.md](docs/README.md) — 設計文書の索引

**このファイルに規約を書き写さない。** 写した瞬間に二重管理になり、片方だけ古くなる。
ここは入口であって、規約の置き場ではない。

## 交代するときの手順

**引き継ぐ側（作業を終えるとき）**

1. 作業中のものをコミットして**プッシュする**。手元だけに残さない
2. `docs/STATUS.md` を更新する — 進行中の作業、判断待ち、次の一手
3. 設計を変えたなら、設計文書も同じブランチに入れる（CLAUDE.md「設計文書の更新は実装と同じブランチに入れる」）

**引き継ぐ側（作業を始めるとき）**

1. `git status` と `git log --oneline -10` を見る
2. `docs/STATUS.md` を読む
3. **未コミットの変更があれば、内容を読んでから触る。** 黙って上書き・破棄しない
4. リモートのブランチを確認する（`git branch -r`）。前任が途中のブランチを残していることがある

## エージェント固有のもの

| 場所 | 誰が読むか | 扱い |
|---|---|---|
| `CLAUDE.md` | Claude Code が自動で読む | **規約の正本。** 他のエージェントも必ず読む |
| `AGENTS.md`（これ） | Codex が自動で読む | 入口。規約は書かない |
| `.claude/skills/*` | Claude Code のみ | 内容は設計文書にもある。**ここにしかない規約を置かない** |

`.claude/skills/` に新しい判断基準を書いたら、**設計文書にも反映する**。
Claude Code でしか読まれないため、Codex の番になった途端に失われる。

## 検証コマンド（どのエージェントでも同じ）

```bash
ruff check batch && mypy batch && pytest batch/tests -q
cd api && npx tsc --noEmit && npx eslint . && npm test
cd web && npx tsc --noEmit && npx eslint . && npm run build
```

CI（`.github/workflows/ci.yml`）が同じものを回す。**手元で通してからプッシュする。**
