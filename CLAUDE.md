# B.PREDICT

B.LEAGUE PREMIER の試合について、勝敗確率・予想スコア・個人スタッツを統計モデルで予測し、判断根拠と自身の的中率とともに公開する非公式ツール。

**プロダクト名は仮称。Phase 0 完了時までに確定させる（商標調査を含む）。**

## 設計文書

実装の前に、該当する文書を読むこと。設計と異なる実装をしてはならない。仕様に疑問があれば実装を止めて確認する。

- @docs/requirements.md — 要件定義。スコープ、制約、特徴量の採否、法務、受け入れ基準
- @docs/design-basic.md — 基本設計。構成、データフロー、サブシステム、画面、テスト戦略
- @docs/design-detail.md — 詳細設計。DDL、API仕様、特徴量定義、学習手順、テスト

## 技術スタック

| レイヤ | 技術 | バージョン | ディレクトリ |
|---|---|---|---|
| フロント | Next.js（**静的出力**） / TypeScript / Tailwind CSS | **Next.js 16 / Node 20.9+ / TS 5.1+** | `web/` |
| 予測データ配信 | **バッチが書き出す静的JSON** | — | `web/public/data/` |
| API | Cloudflare Workers / Hono / TypeScript | — | `api/` |
| DB | Cloudflare D1 (SQLite) | — | `db/` |
| バッチ | Python 3.12 / LightGBM / pandas | — | `batch/` |
| **学習入力** | **Parquet スナップショット（D1 は読まない）** | — | `batch/snapshot/` |
| CI・定期実行 | GitHub Actions | — | `.github/workflows/` |

**Next.js 16 の非互換に注意する。**

- `params` / `searchParams` は **Promise**。`const { date } = await params`
- **`next lint` は削除された。** `eslint .` を直接呼ぶ。`next build` はリントしない
- ESLint は **Flat Config**（`eslint.config.mjs`）。`.eslintrc.json` を作らない
- Turbopack が既定

## コマンド

```bash
# フロント（next lint は Next.js 16 で削除されている。eslint を直接呼ぶ）
cd web && npm ci && npm run dev
cd web && npx tsc --noEmit && npx eslint . && npm run build
find web/out -type f | wc -l             # 18000 以下であること

# API
cd api && npm ci && npm run dev          # wrangler dev
cd api && npm run test && npx tsc --noEmit
cd api && npm run deploy

# DB
wrangler d1 migrations apply bpredict --local
wrangler d1 migrations apply bpredict --remote
wrangler d1 execute bpredict --local --file=db/seeds/dev/seed.sql

# バッチ
ruff check batch && mypy batch
pytest batch/tests -v
pytest batch/tests/test_leakage.py -v    # 特徴量を触ったら必ず実行する

# ローカルで日次バッチを試す（本番D1に書かない）
API_BASE_URL=http://127.0.0.1:8787 INGEST_TOKEN=dev \
  python -m batch.jobs.daily_ingest --dry-run
```

---

## 絶対ルール

以下に違反する実装は、他がどれだけ良くても受け入れられない。

### 1. データリークの禁止

**特徴量は「試合開始時点で確定している情報」のみで構成する。**

- 特徴量関数は必ず `as_of` を引数に取り、参照条件に **`finished_at <= as_of`** を含める（`tipoff_at` ではない。17:05開始・19:05終了の試合が19:05開始の試合に混入する）
- 対象試合自身の `team_game_stats` / `player_game_stats` / `games.attendance` を参照しない
- `status = 'SCHEDULED'` の試合を集計に含めない
- チーム所属の判定に「現在の所属」を使わない。`player_game_stats.club_id`（実績）か `player_seasons`（当該シーズン断面）を使う
- 試合後にしか判明しない値は、必ず「過去試合の集計」に変換してから使う
- 検証は時系列分割。ランダムシャッフル分割を使わない
- 特徴量を追加・変更したら `pytest batch/tests/test_leakage.py` を実行する
- **検証精度が 85% を超えたらリークを疑う。** 喜ばずに原因を調べる

リーク検証は「`as_of` をずらす」ではなく「**DBを撹乱して `as_of` 固定で不変であること**」で行う。前者は論理が逆で、`as_of` を無視する実装だけを通してしまう。

### 2. 予測を後から書き換えない

- 試合開始時刻を過ぎた予測（`is_final = 1`）は**一切更新・削除しない**。DBトリガで禁止されている
- 再推論は旧行を `is_active = 0` にして新行を `revision + 1` で追加する。UPDATE で上書きしない
- **非活性化と挿入は必ず単一の `batch()` に入れる**（D1 にはリクエストを跨ぐトランザクションがない）。親（`predictions`）と子（`player_predictions` ほか）の非活性化も同じ `batch()` にまとめる
- **freeze も親子をまとめて単一の `batch()` で行う。** `is_final` 列を持つのは `predictions` と `player_predictions` の2つで、**子を先・親を後の順に UPDATE する。この順序は必須である**（子4テーブルすべてに親参照トリガがあり、親を先に確定させると子の `0 → 1` が拒否されて freeze 自体が失敗する）
- `/internal/predictions` は `tipoff_at <= now` の試合を 409 で拒否する。このガードを外さない
- 推論対象は `tipoff_at > now` の試合に限定する
- 的中率の算出は `is_final = 1` の行のみを使う

**凍結の範囲に例外を設けない。** `predictions` だけでなく `player_predictions` / `prediction_reasons` / `prediction_team_targets` / `prediction_model_bundle` にも同じトリガを置く。根拠やスタッツだけ後から書き換えられるなら、予測の不変性という主張そのものが成立しない。**子4テーブルはすべて親参照トリガで守る**（`player_predictions` は自身の `is_final` と親参照の二重）。自テーブルの列だけに頼ると、freeze が子を取りこぼしたときに関門が消える。表示上の誤り（ラベルの誤字など）が見つかっても**過去の行は修正せず**、以後の生成ロジックだけを直す。

### 3. D1 への書き込みは Workers 経由に一本化する

**D1 REST API を直接叩く経路を作らない。** REST 直叩きを許すと、Zod 検証・認可・tipoff ガード・`is_final` 保護がすべて迂回可能になり、このプロダクトの中核価値を守る関門が存在しなくなる。`CF_API_TOKEN` はマイグレーション専用（Edit スコープ）。障害調査で手元から SELECT を流す場合は、**読み取り専用スコープの別トークンを手元にのみ置く**（CI とリポジトリに入れない）。

**バッチは D1 を「入力データ」として読まない。** このルールの射程は次のとおり。

| 区分 | 扱い |
|---|---|
| **特徴量生成・学習・推論の入力データ** | **スナップショット（`batch/snapshot/*.parquet`）のみ。** D1 を読まない。「一括エクスポートして読む」経路も作らない |
| 運用上の読み取り（取得済みID照会、結果照合、モデル artifact の読み出し、現行モデルの評価値） | Workers の `/internal/*` に GET を置いて行う。件数は1回あたり数行〜数百行で、読取枠に影響しない |
| D1 REST API の直叩き | **全面禁止**（区分を問わない） |

入力データに例外を一度でも許すと、逐次クエリへの退行を設計で止められなくなる。逆に運用上の読み取りまで禁じると、backfill の再開判定・結果照合・モデルの読み出しが実装不能になる。**両者を混ぜないこと**が本ルールの要点である。

スナップショットは `daily_ingest` が D1 へ書くのと同じデータから書き出し、`MANIFEST.json` の行数と SHA256 で整合を検証する。Elo（`team_ratings`）もバッチが計算してスナップショットへ書き、同じ値を `/internal/ratings` 経由で D1 にも送る。

**1リクエストあたりの D1 クエリ数を50以下に保つ。** Free プランの上限であり、このプロダクトで最初に当たる制約である。1文に詰められる行数はバインドパラメータ上限100で決まるため、バッチサイズは

```
max_rows_per_request = floor(100 / 列数) × 40
```

で**テーブルごとに**算出する。列数は DDL の**全列数**（バインドしうる最大）で数える。一律500件にしない（`player_predictions` は31列あり、500件で167文必要になる）。CPU 10ms は実測1.34msで余裕があり、制約ではない。**バッチサイズは CPU ではなくクエリ数で決める。**

### 4. 機密情報をリポジトリに入れない／漏らさない

**このリポジトリは public であり、Actions のログも全世界から閲覧できる。**

- トークン類をコード・設定・ログに書かない。GitHub Secrets / Wrangler Secret から読む
- **例外オブジェクトをそのままログ・DBに入れない**（URLに含まれるIDが漏れる）。型名と自前の短いメッセージに限定する
- `/api/v1/health` に `error_message` を含めない
- `.env` の実ファイル、**取得した生HTML**、学習済みモデルバイナリをコミットしない
- ワークフローの `permissions` は**既定を `contents: read`** とし、静的JSONとスナップショットをコミットする `daily-ingest` と `gameday-update` の2つだけ `contents: write` にする。`permissions` を書かないワークフローを作らない
- D1 クエリは必ず `prepare().bind()` を使う。文字列連結でSQLを組まない

`D1_DATABASE_ID` は機密ではないため `wrangler.toml` に直書きしてよい。

### 5. 無料枠を超える構成にしない

年間運用費は原則ゼロ、上限5,000円（Apple Developer 年会費を除く）。**上限のない従量課金構成は採用しない。**

- Cloudflare は無料プランを維持する。Workers Paid（年額約9,000円）へ移行しない
- **ISR を使わない。** Pages 上の Next.js は ISR が成立せず、成立させるには R2/KV（従量課金）が要る
- **主要導線（今日の予測・試合一覧）は Workers も D1 も経由させない。** バッチが書き出した静的JSONを配信する
- **特徴量生成と学習で D1 を読まない。** 入力はスナップショットのみ（絶対ルール3）
- **静的生成は直近5シーズンまで。** Pages の1デプロイ20,000ファイル上限に対し、全10シーズンでは 20,184 ファイルで超過する。CI で `out/` のファイル数が18,000以下であることを検査する
- **`model_versions.artifact_text` は1.5MB以下。** D1 の1行上限は2,000,000バイト。`num_boost_round` の上限は1,200、`num_leaves` を7から増やす変更はサイズ再測定とセットでのみ行う
- 公開URLの空間に上限を設ける（シーズン範囲外の日付は D1 到達前に404）
- 有料サービス・有料APIを提案・導入しない。必要と判断したら実装せず相談する

独自ドメイン（年1,000〜1,500円）は例外として認める。WAF レートリミットを使うために必要で、上限内に収まる。

### 6. スクレイピングの作法を守る

- リクエスト間隔は最低3秒＋ジッタ。並列取得をしない
- **日次リクエスト上限3,000、レスポンスサイズ上限5MB**をクライアント側で強制する
- User-Agent に識別名と連絡先URLを含める
- 429 / 503 を受けたら**スクレイピング区間のみ**中止する。内部処理（照合・Elo・推論・書き出し）は継続する
- **`robots.txt` と利用規約をハッシュ照合し、変更があればジョブを中止して確認を求める**
- 取得したHTMLを保存・コミットしない。値域検証を通してから保存する
- **`PARTIAL` で終わったジョブは exit 1 にする**（exit 0 だと失敗通知に乗らず放置される）

### 7. 著作権とプロダクトの性格を守る

- 表示するのは試合日程・スコア等の事実データのみ。記事本文・画像・ロゴを使わない
- **fixtures には取得した本文をそのまま置かず、構造のみを模し値をダミーに置換した合成データを置く**（HTML でも埋め込みJSONでも同じ）。生のレスポンス本文の public リポジトリへのコミットは、「事実データだから」という本プロジェクトの立論が一切通用しない唯一の行為
- 取得した生データを一括配信・公開API化しない
- 金銭・ポイントを賭ける機能、ブックメーカーのオッズ参照・外部リンクを実装しない
- **広告を導入する場合、賭博カテゴリのブロック設定を導入と同時に行う。** ブックメーカーとのアフィリエイト契約を結ばない
- 免責・出典・プライバシーポリシーの表示を削除・後回しにしない
- **根拠ラベルに選手個人の能力・資質への評価を含む表現を使わない**（可:「出場時間の減少」/ 不可:「不調」「衰え」「戦力外」）

---

## 実装方針

### 責務の分離

- `batch/scraper/` は HTTP の作法のみ。レスポンス本文を解釈しない
- `batch/parser/` は**レスポンス本文の解釈**と値域検証のみ。HTTP を知らない
- サイト構造の変更を `parser/` の修正だけで吸収できる状態を保つ

**ボックススコアは HTML の表ではなく `<script>` 内の埋め込みJSONで配信される**（Phase 0 で確認）。`parser/` は本文から JSON を取り出し、**対応表にある列だけ**を抜き出す（詳細設計 4.4）。JSON 全体を保存しない（審判名とプレイバイプレイが含まれる）。取り込み前に5つの正規化を必ず通す。

0. 試合一覧を**大会区分（`event`）で絞る。** 取り込むのは `2`（リーグ戦 → `REGULAR`）と `3`（チャンピオンシップ → `PLAYOFF`）だけ。`4` 残留プレーオフ / `5` オールスター / `11` 入替戦 / `20` アーリーカップは入れない
1. `PeriodCategory` が **18（試合通算）** の行だけを取る。他を入れると行数が7倍になる
2. `PlayerID` が空の**チーム集計行**を選手行から分離する
3. 数値が `0` ではなく**空文字**で入る場合があるため、値域検証の前に None へ正規化する
5. **`StadiumCD` を文字列へ正規化する。** 2016-17 は整数 `3`、2025-26 は文字列 `"169"`。放置すると同じ会場が2行に分裂する

**オールスターゲームを取り込むと Elo が壊れる。** 選抜チーム同士の対戦であり `clubs` に存在しないチームが現れる。入替戦は相手が B2 のクラブで対戦相手を解決できない。**絞り込みはラベルではなく値で行う**（`event=2` のラベルは 2026-27 で「B1リーグ」から「B.PREMIER」に変わっている）。区分は `games.competition` と `team_games.competition` に保持し、取り込みの絞り込みだけで済ませない（区別を捨てると特徴量 #17 が算出できず、復元には backfill のやり直しが必要になる）。

**公式サイトの `TeamID` はシーズン・改称・リーグ再編をまたいで不変**（Phase 0 で確認）。`club_source_ids` は本ソースでは1:1になるが、ID体系が変わったときの継ぎ目として維持する。**過去シーズンの正式名称は `TeamNameJ`（当時の名称が入る）から取る。** 年度別のクラブ一覧ページは存在しないため、`club_seasons` は `seed_master` ではなく backfill が試合データから作る。

**会場も公式IDを使う。** `venues.id` は試合JSONの `StadiumCD`（全シーズンに存在する）。独自採番も会場名による名寄せもしない。**会場の集合は事前に列挙できない**（代替会場が多く毎年変わる）ため、会場行も backfill が試合データから作る。`seed_master` が投入するのは `seasons` / `clubs` / `club_source_ids` だけである。

**収容人数と緯度経度は公式サイトにない。** 座標は `/arena_detail/` の住所を国土地理院でジオコーディングし、**1回だけ解決して CSV に固定する**（実行時に外部サービスへ依存しない）。収容人数は**手入力**で、2026-27 の26クラブのメイン会場から埋め、代替会場は NULL のまま進める。**「入場者数の最大値を収容人数とみなす」自動化をしない** — 未来の試合から値を作ることになり絶対ルール1に触れ、観客制限期間では判定が循環する。

### 冪等性

バッチは何度実行しても同じ結果になること。

- 試合は**公式試合IDを主キー**に upsert する（自然キーに `game_date` を含めない。延期で別レコードになる）
- エントリー情報は取得ごとに**全行を洗い替える**（追加のみだと推定行が残り `暫定` が解除されない）
- Elo は差分更新せず対象期間を再計算する。backfill 後は `--full` で全期間を洗い替える
- 原子性が必要な操作は単一 `batch()` に入れる

### 失敗時の挙動

- 個別試合の失敗はスキップしてジョブを継続する
- 値域検証エラーは当該試合をスキップする
- パースエラーが連続3件でジョブを中止し `PARTIAL` + exit 1
- 推論に失敗したら前回の静的JSONを維持し、画面に更新遅延を表示する
- 学習結果が採用基準を満たさなければ、新モデルを有効化せず現行を使い続ける

### モデル

- **Accuracy を採用ゲートにしない。表示専用。** ホーム勝率60%の下では良い確率モデルでも Accuracy はベースラインと並ぶ。ゲートは Brier（有意 かつ 差0.003以上）と ECE
- **`n < 500` では比較そのものを行わない。** 理想モデルとベースラインの Accuracy 差の SD は n=200 で 0.041 あり、`+0.03` という基準はノイズと区別できない
- **ECE の閾値を固定値にしない。** 完全較正モデルでも ECE 95%点は n=200 で 0.116、n=1,200 で 0.048 と n に強く依存する。モデル自身の予測分布からブートストラップでノイズフロアを推定し、その1.5倍を閾値とする
- ベースラインは3段（ホーム必勝 / **Elo単体ロジスティック回帰** / 全特徴ロジスティック回帰）。2を超えられなければ LightGBM を使う理由がない
- 乱数シードを必ず固定する。固定しないと Brier が ±0.003 ぶれ、採用判定が乱数に支配される
- 較正は out-of-fold で fit する。**そもそも必要かを先に測る**（logloss 目的の LightGBM は元々ほぼ較正されている）
- 予想スコアは**得点差と合計得点**から導出する。各チーム得点を独立に回帰すると勝率と矛盾する
- **勝率は2経路（Winner直接 / Marginから導出）を両方実装する。** Phase 0 で Brier と ECE を比較して採用を決める。勝手にどちらかだけ実装しない
- **月次再学習の検出力は低いが、ゲートを緩めない。** 真の差0.03の検出確率は n=1,200 でも54.8%。見逃す方向に倒すのは意図的な設計であり、緩めれば誤差し替えを招く
- 精度が出ないときに木を深くしない。まず特徴量を見直す

### 個人スタッツ

- **シュートは「試投数 + 成功率」で予測し、成功数は導出する。** 成功数と試投数を別々に予測・スケールすると `成功数 > 試投数` が生じ、それを防ぐクリップが整合化の収束を壊す（実測で失敗ケースの73%が FT 成功率）
- **モデルが出す成功率を画面にそのまま出さない。** 表示する率は `FGM / FGA` という導出値で、必ず分数と併記する。試投数が閾値未満なら率を出さない
- **FG は 2P と 3P の合計として導出する。** FG を予測して分解すると `FGM = 2FGM + 3FGM` が崩れる
- **得点は恒等式（`2FGM×2 + 3FGM×3 + FTM`）で導出する。** 独立に予測しない
- **＋/－ を予測しない。** 1試合の個人プラスマイナスは MAE 9〜12点規模で、「チーム得点差 × 出場時間シェア」以上の情報を持たない。実績のみ表示する
- **選手予測は必ずチーム予測に整合化してから保存する。** 独立予測のままだと5人の得点合計が予想スコアと一致せず、同じ画面に矛盾した数字が並ぶ
- **整合化の成功率はロジット空間で一律シフトする（`brentq` で解く）。クリップを使わない。** 倍率スケール方式は収束しない（実測で項目誤差最大24.7%、得点誤差4.42点）。ロジット方式は得点誤差ゼロ・制約違反ゼロ。反復は3回
- **チーム側の目標値も「試投数 + 成功率」で持つ。** 各項目を独立に生成すると約2%の確率で実行不能な目標（`FT成功数 > FT試投数`）が生まれ、整合化が原理的に成立しない
- **実行不能な目標に出会ったら例外を投げる。** 無言でクリップして辻褄を合わせない。当該試合の個人スタッツを破棄し、画面に算出不能と表示する
- **カウント11項目は per-minute で学習する。成功率3項目は per-minute にしない**（率は時間当たりの量ではない）。成功率は試投数を重みにしてシュリンク済みの率を学習し、出力を `clip(0.01, 0.99)` に収める
- `P(出場) < 0.5` の選手は表示しない
- ST / BS は MAE が平均値と同水準で、実質「平均を出しているだけ」になる。隠さず誤差目安を併記する

### UI

- 既定テーマはライト。ダークへ切替可能。選択は `localStorage`（try/catch 必須）
- 色はすべて CSS カスタムプロパティ経由。コンポーネントに色を直接書かない
- **`--text-3` は本文用、`--text-4` は罫線など非テキスト専用。** 混同しない
- 両テーマでコントラスト比 4.5:1 以上。**CI のトークン単体テストで検証する**
- 数値には `tabular-nums` を適用する
- **勝率は必ず両チーム分を表示する。** 圧縮カードでも省略しない
- 予想スコアは整数（MAE 8〜10点に対し小数第1位は精度の誤認を招く）
- 的中率には母数を併記する
- **個人スタッツは段階開示。** 初期は MIN / PTS / TR / AS の4項目、展開でフルボックススコア
- **率は分数と併記する。** 単独で `48.9%` と出さない。試投数の予測値が閾値未満なら率を出さず分数のみ
- 誤差の目安は主要4項目にのみ併記する（全項目に出すと画面が埋まる）
- `%` は勝率専用。SHAP の寄与度をパーセントで表示しない（ログオッズ空間であり確率への加算ではない）
- 見出し要素で文書構造を作る。装飾目的で `<s>` `<i>` `<em>` を使わない
- タブ・ボタンは最低44×44px
- `dangerouslySetInnerHTML` を使わない
- チャートはライブラリを使わずインラインSVGで実装する
- フォントはシステムフォントスタック。Webフォントは数字用1ウェイトのみ

### 状態の明示

ユーザーに対して、予測の確からしさを隠さない。

- 出場者未確定なら「暫定」
- 消化試合5未満なら「序盤のため精度が安定しません」
- `status = 'SUCCESS'` の最新から24時間以上経っていれば更新遅延バナー（**`status` を見ること**。見ないと毎日失敗しても遅延と判定されない）
- 的中率は良い時も悪い時も同じ場所に表示する
- **外れた試合を隠さない。** 結果画面で実績と対比し、その確率帯の通算的中率を併記する

---

## 作業の進め方

1. 新機能の実装前に `/grill-me` で仕様の曖昧さを潰す
2. 設計文書と矛盾する実装をしない。設計を変えるべきなら、先に文書を更新する
3. 特徴量を追加・変更したら `pytest batch/tests/test_leakage.py` を実行する
4. 未決事項（`docs/design-basic.md` 9章）に触れる作業では、決め打ちせず確認する
5. Phase 0 の検証結果で前提が変わる可能性があることを意識する

### 文書の優先順位と矛盾時の扱い

1. `CLAUDE.md`（このファイル）の絶対ルール
2. `docs/requirements.md`
3. `docs/design-basic.md`
4. `docs/design-detail.md`
5. `.claude/skills/*/SKILL.md`

**文書間に矛盾を見つけたら、実装を止めて報告する。** 優先順位は「どちらが正しいか」の推定材料であって、**低い側を黙って上書きしてよいという意味ではない**。上位文書が正しいと判断した場合でも、下位文書を先に直してから実装する。

### 勝手な仕様補完をしない

設計文書に書かれていない仕様が必要になったら、**推測で埋めずに止めて確認する**。以下は特に埋めてはならない。

- 閾値・係数・上限値（表示する試投数の閾値、シュリンクの `k`、σ の値など）
- エラー時の挙動（握りつぶすか、落とすか、フォールバックするか）
- 表示文言（ユーザーに見える日本語は設計の一部である）
- 「とりあえず動く」ためのデフォルト値

「実装しながら決めた値」は文書に残らず、後から理由を復元できない。

### 設計から外れる必要が出たとき

1. 実装を止める
2. 何が設計どおりにできないか、なぜかを具体的に示す
3. 代替案を2つ以上出し、それぞれの影響（無料枠・精度・不変性・法務）を述べる
4. 承認を得てから、**先に設計文書を更新し**、その後に実装する

設計を後追いで文書化しない。文書が実装の後ろを走り始めた時点で、設計文書は参照されなくなる。

### マイグレーションは追記のみ

- `db/migrations/*.sql` は**適用済みのファイルを編集しない**。必ず新しい番号のファイルを追加する
- 適用済みファイルを書き換えると、ローカルと本番でスキーマが分岐し、`migrations apply` が沈黙して壊れる
- `DROP TABLE` / `DROP COLUMN` を含むマイグレーションは、事前に確認を取る
- テストのスキーマは `db/migrations/*.sql` をそのまま in-memory SQLite に適用する（二重管理しない）

### 時刻の扱い

| 場所 | 形式 |
|---|---|
| DB の全時刻列 | **UTC**。ISO 8601（`2026-09-22T10:05:00Z`） |
| API のレスポンス | UTC のまま返す |
| 画面表示 | **JST** に変換して表示する |
| cron | **UTC** で書く。JST から9時間引く。日付をまたぐ場合は曜日指定もずらす |

**`game_date` は「その試合の JST における暦日」と定義する。** `tipoff_at`（UTC）から機械的に導けるが、UTC の日付とは一致しないため、必ず JST へ変換してから日付を取る。19:05 JST 開始の試合は UTC では 10:05 で同日だが、この定義を曖昧にすると「今日の試合」が日によってずれる。

### 生成物を手で編集しない

以下はコマンドが生成する。手で直さない。直したい場合は生成元を直す。

| 生成物 | 生成元 |
|---|---|
| `web/public/data/*.json` | `batch.jobs.*` の書き出し |
| `batch/snapshot/*.parquet` / `MANIFEST.json` | `daily_ingest` |
| `web/out/` | `next build` |
| `package-lock.json` | `npm install`（`npm ci` は変更しない） |

### 依存を追加するとき

追加の前に、次をすべて確認して報告する。

1. **本当に必要か。** 標準ライブラリ、または数十行の自前実装で足りないか
2. **無料枠への影響。** フロントならバンドルサイズ（`/accuracy` に Recharts を入れると gzip 後100〜300KB増）、Workers なら CPU 時間
3. **ライセンス。** public リポジトリで配布可能か
4. **メンテナンス状況。** 最終更新、未解決の脆弱性
5. **バージョン固定。** Python は `==`、npm は `package-lock.json` をコミット

チャートライブラリ、日付ライブラリ、状態管理ライブラリは**既に検討して見送っている**。再提案する場合は上記に加えて、なぜ以前の判断が変わるのかを述べる。

### 止まるべき条件

次のいずれかに当たったら、作業を続けずに報告する。

- 文書間に矛盾がある
- 設計にない仕様を決める必要がある
- 無料枠を超える構成が必要に見える
- 外部サイトの構造が設計の前提と違う
- 検証精度が 85% を超えた（リークの疑い）
- 整合化が収束しない、または `InfeasibleTargetError` が頻発する
- 法務・著作権に関わる判断が必要になった

## 用語

| 用語 | 意味 |
|---|---|
| 暫定 (provisional) | 出場者未確定の状態で算出された予測 |
| 確定 (final) | 試合開始をもって凍結された予測。変更不可 |
| as_of | 特徴量が参照してよい情報の上限時刻（= その試合の `tipoff_at`） |
| data_as_of | 推論実行時点で DB にあった最新試合の終了時刻。検証と本番の鮮度差を検出する |
| revision | 同一試合・同一モデルの推論回数。世代通番 |
| Brier Score | 確率予測の精度指標。0に近いほど良い。**主要な改善指標** |
| ECE | 較正誤差。等頻度10ビン、ビンあたり最低50サンプル。**閾値は n 依存** |
| ノイズフロア | 完全に較正されたモデルでもサンプリングノイズで出る ECE の水準 |
| ベースライン | ホーム必勝 / Elo単体ロジスティック回帰 / 全特徴ロジスティック回帰の3段 |
| VOID | 中止・延期により的中率の母数から除外された予測 |
| 整合化 (reconciliation) | 選手予測の合計をチーム予測に一致させる計算。ロジット空間シフトで行う |
| スナップショット | `batch/snapshot/*.parquet`。学習・特徴量生成の唯一の入力 |
| 経路A / 経路B | 勝率の導出方法。A = Winner 直接、B = Margin から `Φ(margin/σ)` |
| `game_date` | その試合の **JST における暦日**。`tipoff_at`（UTC）から変換して求める |
