# テスト用の依存。バージョンは == で固定する（CLAUDE.md「依存を追加するとき」）。
# 実行時の依存は batch/requirements.txt に置く（工程5でスクレイパを作るときに追加）。
#
# Python 3.12 が必須（docs/requirements.md 7章）。
#   python3.12 -m venv .venv && . .venv/bin/activate
#   pip install -r batch/requirements-dev.txt
#   pytest batch/tests -v
pytest==9.0.3
