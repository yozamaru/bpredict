# 設計文書

| 文書 | 内容 |
|---|---|
| `requirements.md` | 要件定義。スコープ、制約、特徴量の採否、法務、受け入れ基準 |
| `design-basic.md` | 基本設計。構成、データフロー、サブシステム、画面、テスト戦略 |
| `design-detail.md` | 詳細設計。DDL、API仕様、特徴量定義、学習手順、テスト、運用 |
| [`scraping-sources.md`](scraping-sources.md) | B.LEAGUE取得元の調査・AI間の引き継ぎ。対象URL、確認した構造、未確認事項、設計との差（仕様変更の承認ではない） |
| [`scraper-parser.md`](scraper-parser.md) | 工程5の実装の利用方法。取得前確認の設定、カナリアの運用、解析の契約、確認コマンド |

**版数 1.20。** v1.1 で9領域レビューの指摘30件を反映、v1.2 で個人スタッツをフルボックススコアに拡張、v1.3 で実装前検証（15項目のうち9件を実行、6件を Phase 0 へ）の結果を反映、v1.4 で文書レビューの指摘（矛盾11件・未定義10件）を反映、v1.5 で実装着手前の再点検の結果を反映、v1.6 で Phase 0 の実地確認（ボックススコアの配信形式）を反映、v1.7 で凍結トリガの網羅を完成させ、v1.8 で Phase 0（P0-5）のクラブ対応関係の結果を反映し、v1.9 で会場マスタの出典を確定させ、v1.10 で工程2の前提を確定させ、v1.11 で工程3の CI を実態に合わせ、v1.12 で工程4a（Workers API の土台）を実装して工程2の D1 投入を完了させ、v1.13 で工程4b（残りの `/internal/*`）を実装し、v1.14 で工程5（スクレイパ・パーサ）を実装し、v1.15 で工程11a（デザインシステムと画面の骨組み）の実測を反映して初期JS の上限を 180KB・静的生成の範囲を直近3シーズンに改め、v1.16 で工程7（特徴量生成とリーク検証）を実装し、v1.17 で工程6のワークフロー（`backfill.yml`）を追加し、v1.18 で Elo（`batch/ratings/`）と `recompute_ratings` を実装し、v1.19 で `venue_revisions` を派生テーブルに変更し（`games.venue_name_at_game` を追加）、**v1.20 で内部APIのクライアントに User-Agent を足した**（本番での取り込み開始時に判明）。

**v1.11〜v1.13 が触ったのは `design-basic.md` と `design-detail.md` の2つだけで、`requirements.md` は 1.10 のまま**だった。CI の構成・工程の割り当て・実装の進捗を反映しただけで、要件（受け入れ基準 A-10 / A-12 / A-14 を含む）は変わっていない。

**v1.14 と v1.15 では `requirements.md` も上げた（1.11 → 1.12）。** 工程5の実地確認で、要件 5.2 の取得ルールに実装できない記述（429/503 で「当該ジョブを即座に中止」）と、再確認で値が違った記述（`robots.txt` の Disallow 対象数）があったためである。**受け入れ基準は変えていない。**

## 文書の優先順位

矛盾を見つけたときの判断材料。**低い側を黙って上書きしてよいという意味ではない。**

1. `../CLAUDE.md` の絶対ルール
2. `requirements.md`
3. `design-basic.md`
4. `design-detail.md`
5. `../.claude/skills/*/SKILL.md`

**文書間に矛盾を見つけたら、実装を止めて報告する。** 上位が正しいと判断した場合でも、下位文書を先に直してから実装する。

## 更新ルール

- 設計と異なる実装をしない。設計を変えるべきなら、先に文書を更新する
- 実装と文書のずれは `/sync-docs` で検出する
- 未決事項は `design-basic.md` 9章に集約する
- Phase 0 の実機検証項目は `requirements.md` 11章に集約する
- スキル（`.claude/skills/`）は判断基準と禁止事項を扱い、数値やキー名は本文書を正とする

### 数値表の複写について（重要）

上の方針にもかかわらず、**同じ数値表が複数箇所に実在する**。片方だけ直すと文書間が静かに分岐するため、これらを変更するときは**全箇所を同時に直す**こと。

| 内容 | 複写されている場所 |
|---|---|
| 無料枠の一覧（Workers / D1 / Pages / WAF） | `../CLAUDE.md` 絶対ルール5 / `requirements.md` 7章 / `design-basic.md` 7.5 / `../.claude/skills/cloudflare-ops/SKILL.md` |
| バッチサイズ（`floor(100/列数)×40` と各テーブルの上限） | `../CLAUDE.md` 絶対ルール3 / `design-detail.md` 3.4 / `../.claude/skills/cloudflare-ops/SKILL.md` |
| 整合化の手順と1,500ケースの実測表 | `requirements.md` 6.8.5 / `design-basic.md` 2.3 / `design-detail.md` 2.4 / `../.claude/skills/model-training/SKILL.md` |
| ECE ノイズフロア表（n=200/500/1,200/3,000） | `requirements.md` 6.4 / `design-detail.md` 4.6 / `../.claude/skills/model-training/SKILL.md` |
| Accuracy の検出力表（理想モデル vs ベースライン） | `requirements.md` 6.5 / `design-detail.md` 4.6 / `../.claude/skills/model-training/SKILL.md` |
| 採用基準（`n≥500` / `0.003` / `ECE_FLOOR_K=1.5`） | `requirements.md` 6.5 / `design-detail.md` 4.6 / `../.claude/skills/model-training/SKILL.md` / `../.claude/commands/train.md` |
| artifact サイズ表（1.5MB / `num_leaves` × 木の本数） | `design-basic.md` 2.3 / `design-detail.md` 1.6 / `../.claude/skills/model-training/SKILL.md` |
| 静的生成の範囲（20,000 / 18,000 / **直近3シーズン**・1ルート5ファイル） | `requirements.md` 8.2 / `design-basic.md` 2.5・7.5 / `design-detail.md` 5.6 / `../.claude/skills/ui-implementation/SKILL.md` |
| デザイントークン一式 | `design-basic.md` 6.1 / `design-detail.md` 5.4 / `../.claude/skills/ui-implementation/SKILL.md` |
| 画面文言（暫定・序盤・更新遅延・空状態） | `requirements.md` 8.4〜8.5 / `design-basic.md` 5.3 / `../.claude/skills/ui-implementation/SKILL.md` |
| 受け入れ基準 A-01〜A-18 | `requirements.md` 12章 / `../.claude/commands/verify-release.md` |

統合は将来の課題として残す。**統合していない以上、変更時に全箇所を直す作業は省略できない。**
- 設計にない仕様を推測で埋めない。止めて確認する

## 確定事項

grill-me（19問）と9領域レビューにより確定した主要な決定。

| 決定 | 内容 |
|---|---|
| プロダクト性格 | モデル予測型（ユーザー入力なし） |
| リリース順序 | Web先行 → iOS、Android は PWA のみ |
| データ調達 | 公式サイトを低頻度スクレイピング（日次上限3,000） |
| モデル | LightGBM。勝敗 + 得点差/合計 + 個人（ハードルモデル） |
| 配信 | **主要導線は静的JSON。Workers/D1 を経由しない** |
| 書き込み | **Workers の /internal/* に一本化**（REST直叩きを作らない） |
| 対象 | B.PREMIER のみ・全シーズン蓄積 |
| 出力 | 勝敗・勝率・予想スコア・**個人フルボックススコア**・根拠（v1全部入り） |
| 的中率 | モデルバージョン別に公開。**外れた試合も結果画面で対比表示** |
| UI | ライト既定＋ダーク切替 |
| コスト | 年額ゼロ〜5,000円、従量課金なし。独自ドメインのみ例外 |
| リポジトリ | public（機密は Secrets、生HTMLはコミット禁止） |

## レビューで変更した主な点

| # | 変更 |
|---|---|
| 1 | リーク検証テストを「DB撹乱法」に差し替え（旧版はアサーションが逆でリーク実装を通していた） |
| 2 | `predictions` に `revision` を導入（旧版のUNIQUE制約では追記型が成立しなかった） |
| 3 | freeze を独立ジョブ化し、API に tipoff ガードを追加 |
| 4 | ISR をやめ、静的JSON配信へ |
| 5 | D1 の一括エクスポート方式へ（逐次クエリは無料枠を2桁超過） |
| 6 | モデルを D1 に格納（artifact は90日で失効） |
| 7 | 生HTMLのコミット禁止を明文化し `.gitignore` に追加 |
| 8 | 個人情報保護法上の事業者該当を明記し、プライバシーポリシーを必須化 |
| 9 | データモデルを「恒久エンティティ＋断面」に再構成 |
| 10 | 特徴量の区分を修正（天候・直接対戦を見送り、`is_home` を削除） |
| 11 | 較正を out-of-fold へ、ベースラインを3段に、採用基準から Accuracy を除外 |
| 12 | 予想スコアを得点差＋合計から導出（勝率との矛盾を構造的に防止） |
| 13 | `--text-3` のコントラストを基準内に修正、`--text-4` を新設 |
| 14 | 結果画面を追加 |
| 15 | CI・parser-canary を新設 |

## v1.2 での変更（個人スタッツの拡張）

> **注意: この節は改訂履歴であり、現行仕様ではない。** 下表の #2 と #3 は v1.3 で「試投数 + 成功率を予測し、成功数・得点は導出する」に置き換わっている。現行仕様は `requirements.md` 6.8 を参照すること。

| # | 変更 |
|---|---|
| 1 | 予測項目を14カウント（2FGM/2FGA・3FGM/3FGA・FTM/FTA・OR/DR・AS・TO・ST・BS・F・**FD**）に拡張 |
| 2 | **率（FG%等）は予測せず導出**。試投数が少ないと離散値しか取りえないため |
| 3 | **PTS / FGM / FGA / TR は恒等式で導出**。独立に予測しない |
| 4 | **＋/－ と EFF は予測しない**（MAE 9〜12点規模で情報量がない）。実績のみ表示 |
| 5 | **チーム予測への整合化を第4段として追加**。独立予測のままだと合計が予想スコアと一致しない |
| 6 | レートは per-minute で学習（カウントだと出場時間の分散に支配される） |
| 7 | 表示を段階開示に（初期4項目 → 展開でフルボックススコア） |
| 8 | `monthly_train` を120分に延長、選手モデルの fold 数を3に制限 |
| 9 | 受け入れ基準 A-13〜A-15（整合性・率の非表示・恒等式）を追加 |

## v1.3 での変更（実装前検証の反映）

設計段階で15項目の数値検証を計画し、**9件を実行、6件（P0-11〜P0-16）を Phase 0 の実機検証へ送った**（外部レジストリへの到達不可と、実データが必要なため）。**実行した9件のうち2件で設計の誤りが見つかった。**

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **整合化アルゴリズムをロジット空間シフトに全面変更。** 倍率スケール方式は収束しない | 1,500ケースの実測で項目誤差最大24.7%・得点誤差4.42点。失敗の73%がFT成功率のクリップ。ロジット方式で得点誤差ゼロ・制約違反ゼロ |
| 2 | **個人・チームとも「成功数」ではなく「試投数 + 成功率」を予測する構造に変更**（DDL含む） | 独立生成では約2%の確率で `FT成功数 > FT試投数` という実行不能な目標が生じ、整合化が原理的に成立しない |
| 3 | **`prediction_team_targets` テーブルを新設**し、チーム目標に CHECK と恒等式を課した | 同上 |
| 4 | **ECE の閾値を n 依存のノイズフロア基準へ**。`n < 500` では ECE をゲートに使わない | 完全較正モデルの ECE 95%点は n=200 で 0.116、n=1,200 で 0.048 |
| 5 | **A-09 を Brier ベースに書き換え、n の下限を500に**。Accuracy を表示専用に降格 | 理想モデルとベースラインの Accuracy 差の SD は n=200 で 0.041。`+0.03` はノイズと区別できない |
| 6 | **静的生成の範囲を直近5シーズンに限定**し、CI にファイル数検査を追加 | 全10シーズンでは 20,184 ファイルで Pages の20,000上限を超過 |
| 7 | **バッチサイズをテーブルごとに算出**（`floor(100/列数)×40`） | D1 Free の「1呼び出し50クエリ」を超過していた。`player_predictions` は500件で167文（**この行の「9テーブル中6つ」という件数と列数は誤り。v1.4 の #5 で「14テーブル中12」に訂正済み**） |
| 8 | **`artifact_text` に1.5MB上限チェック**を追加、`num_boost_round` を1,200に | D1 の1行上限は2,000,000バイト。`num_leaves=15`・1,500本で 2.56MB |
| 9 | **学習入力をスナップショットに一本化**（D1 からの一括読み出し経路を廃止） | 読取枠の消費と可用性依存を構造的に排除 |
| 10 | **子テーブル（`player_predictions` / `prediction_reasons` ほか）にも凍結トリガ**を例外なく追加 | 根拠だけ後から書き換えられるなら不変性の主張が成立しない |
| 11 | **勝率の導出を2経路とも実装**し、Phase 0 で比較する方針に | どちらが優るかは実データでしか決まらない |
| 12 | **`/games/[date]` → `/schedule/[date]`** | 同一階層に `[date]` と `[id]` は置けない |
| 13 | **Next.js 16 に確定。** `next lint` 廃止 → ESLint CLI、`params` は Promise | Next.js 16 の公式リリースノート |
| 14 | **`gameday_update` を「固定cron 3スロット + 対象絞り込み」に**（動的起動は実現不能だった） | GitHub Actions の `schedule` は動的に変更できない |
| 15 | **WAF レートリミットを無料枠の実仕様に**（10秒カウント / 10秒ブロック） | 無料プランでは1分単位の設定ができない |
| 16 | **Workers CPU 10ms を Critical → Medium に下方修正** | 500行の JSON パース＋検証は実測1.34ms。制約は CPU ではなくクエリ数だった |
| 17 | 受け入れ基準を A-18 まで拡張、品質ゲートに Phase 0 実機検証の完了を追加 | — |
| 18 | `CLAUDE.md` に作業規約を追加（仕様補完の禁止、マイグレーション追記のみ、時刻の扱い、依存追加の手順、止まるべき条件） | — |

検証スクリプトは `../verification/` にある。実データに差し替えれば Phase 0 でもそのまま使える。

## v1.4 での変更（文書レビューの反映）

4文書と `../.claude/` を突き合わせ、矛盾11件・未定義10件・複写4系統を洗い出して反映した。**うち3件は設計判断を要した。**

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **チーム目標の整合化を新設**（`reconcile_team_targets()`）。予想スコアを正とし、チームの成功率3項目を共通のロジットシフトで寄せる。試投数は動かさない | TeamRates と Margin/Total は別モデルで、導出得点が予想スコアと一致する保証がなかった。A-16 が構造的に落ちる状態だった |
| 2 | **絶対ルール3の射程を「入力データ」に限定。** 運用上の読み取りは `/internal/*` の GET を追加して行う（models / games/ingested / predictions/pending / metrics/active） | backfill の再開判定・結果照合・モデル artifact の読み出しが、旧ルールの字義では実装不能だった。D1 REST 直叩きの禁止は不変 |
| 3 | **暫定バッジの文言から具体時刻を除去**（「当日10時ごろ」→「試合当日の午前中」） | `gameday_update` のスロットは JST 11:00 / 13:00 / 16:00 で文言と1時間ずれていた。確定タイミングは U-06 / P0-3 待ちで、どちらの時刻にも根拠がない |
| 4 | **`team_ratings.parquet` をスナップショットに追加**し、Elo の書き出し順序（計算 → parquet → `/internal/ratings`）を明記 | `elo_home` / `elo_away` は `team_ratings` を読むが、スナップショット一覧に無く、絶対ルール3と両立していなかった |
| 5 | **バッチサイズ表を DDL から数え直した。** `player_predictions` 26→31列、`games` 20→23列、`team_game_stats` 20→22列。`games` と `team_game_stats` の上限は 200 → **160** | 列数が実際の DDL と合っておらず、上限が過大だった。列数は全列数で数える規約を明記し、CI 検査（`test_batch_limits_match_schema`）を追加 |
| 6 | **`finished_at` を列として保持**し、推定値には `finished_at_is_estimated` を立てる | 「なければ `tipoff_at + 2h`」の適用箇所が未定義だった。リーク判定の絞り込み条件そのものであり、式が呼び出し側に散ると危険 |
| 7 | **freeze は親子を単一 `batch()` で、子 → 親の順に**行う。`is_final` 列を持つのは `predictions` と `player_predictions` の2つだけで、残り3つの子は親参照トリガで同時に凍結される | 誰がいつ子の `is_final` を立てるかが未定義だった。親を先に立てると子への書き込みがトリガに拒否されるため、順序が結果を変える |
| 8 | **日次2,000件の上限は `predictions` の挿入行数で数える。** 子テーブルは別枠 | 子を含めて数えると `player_predictions` だけで約2,200行/日となり、正常な運用が上限に当たって止まる |
| 9 | **`spectator_restricted` を NULL 許容に変更。** 取り込み時に判定して列に書き、判定不能は NULL。2020-21 / 2021-22 は期間指定で強制的に 1 | 自動判定の実行タイミングと `attendance` 欠損時の扱いが未定義だった |
| 10 | **A-13 の「200分」は1チームあたり**と明記し、テストを side ごとの検証に修正 | 擬似コードが1試合分（=400分）を合計しているように読めた |
| 11 | **`/games/:gameId` のレスポンス形状を試合前後で統一**（`prediction` を `data` 直下へ、`evaluation` は試合前 `null`） | キーの位置が試合の状態で動くと、クライアントが状態ごとに別のパスを持ち、片方だけ壊れる |
| 12 | **`SEASON_REGRESSION` のグリッドに 0.65 を追加**（初期値がグリッド外だった） | `{0.5, 0.6, 0.75, 0.85}` に初期値 0.65 が含まれていなかった |
| 13 | **ワークフロー権限の記述を修正。** 既定 `contents: read`、`daily-ingest` と `gameday-update` のみ `write` | `CLAUDE.md` の「全ワークフローに read」が、最上位文書として下位設計を禁止する形になっていた |
| 14 | **Phase 0 未実行件数を6件（P0-11〜P0-16）に統一。** `P0-16` を実装順序（工程8）に追加。**P0-11 に σ の実測を追加** | 件数が3か所（3件 / 6件 / 4件）で食い違っていた。σ（12〜13点）は他リーグからの見当であり実測値ではない |
| 15 | **`prediction_model_bundle` の最大行数を 32 → 33 に訂正**、`requirements.md` 6.8.1 の「14項目」が第3段の出力数である旨を明記 | 数え直しによる訂正 |
| 16 | **静的JSON の全体像を未決事項 U-09 として明示**（工程9の着手前に確定） | `today.json` 以外のファイル構成・スキーマが未定義で、推測で埋められる状態だった |
| 17 | **数値表の複写箇所を本文書に一覧化**（上記「数値表の複写について」） | 「数値は本文書を正とする」と書きながら、実態としてスキルに数値表が複写されていた |
| 18 | `.claude/skills/cloudflare-ops` のスケジュール節を固定cron 3スロットに全面書き換え、`verification/README.md` の未実行リストを更新 | v1.2 の「当日の最小 `tipoff_at` を基準にスロットを組む」が残っており、v1.3 で実現不能と結論した方式だった |


## v1.5 での変更（実装着手前の再点検）

工程1（DDL確定）に入る前に4文書と `.claude/` を再点検し、**DDL に焼き付いてしまう欠陥2件**と、文書間の不整合2件を直した。あわせて Phase 0 の記録先を用意した。

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **`accuracy_summary.model_version` を `NOT NULL DEFAULT ''` に変更**し、`scope` に CHECK を追加 | 主キー列が NULL 許容だと SQLite は重複を許す。実測で `('OVERALL','all',NULL)` が2行入り、`ON CONFLICT` も衝突を検出しなかった。洗い替えを厳密に守れば事故は起きないが、**主キーが保護になっていない状態**を DDL 確定前に消した。`model_versions.target` と同じ書き方に揃えている |
| 2 | **`updated_at` の適用範囲を「上書きされうるファクトテーブル」に限定**し、`team_games` と `game_entries` が持たない理由を明記 | 「全ファクトテーブルに `updated_at` を持つ」という記述に対し、DDL では5つ中2つが持っていなかった。`team_games` は `games` からの派生、`game_entries` は毎回洗い替えで、どちらも「値が変わった時刻」が意味を持たない。列を足すと実際には更新されていない行の時刻だけが動くため、**記述側を実態に合わせた**（列を足していないのでバッチサイズは不変） |
| 3 | **障害調査の SELECT 用に読み取り専用トークン（`CF_API_TOKEN_RO`）を分離。** 手元のみに置き、GitHub Secrets にも CI にも入れない | `/ingest` と `/review-cost` が `wrangler d1 execute --remote` を使う一方、全文書が「`CF_API_TOKEN` はマイグレーション専用」としていた。禁止しているのは**製品のコードパスとしての REST 直叩き**であって人間の調査クエリではない、という線を明示し、Edit スコープの流用だけを禁じた。あわせて両コマンドを「まず `/api/v1/health` と内部 GET を見る」順序に書き換えた |
| 4 | **ルート `README.md` の免責を要件 4.5.4 の確定文言に差し替え** | 「一切の行為について責任を負いません」という全部免除になっていた。要件 4.5.4 は消費者契約法8条1項によりこの書き方は条項ごと無効になると明記しており、「ただし、故意または重大な過失による場合はこの限りではない」の留保が必要 |
| 5 | **`verification/RESULTS.md` を新設**（Phase 0 の測定結果の記録先） | P0-1〜P0-16 の測定値を書く場所が文書になく、「設計に反映する」とだけ書かれていた。状態・測定日・測定値・反映先を1枚で管理し、この表が埋まるまで工程1へ進まない |
| 6 | v1.4 の反映漏れを修正（要件 A-13 の「1チームあたり」、design-basic 4.2 ステップ7 の `team_ratings` 書き出しと MANIFEST 再生成、実装順序 工程4・9 の完了条件、`verify-release.md` の A-13 / A-16） | 承認済みの決定が一部の文書に波及していなかった。`verify-release.md` はリリース判定に使うチェックリストであり、古いままだと A-16 の新しい条件が検証されない |

## v1.6 での変更（Phase 0 の実地確認）

試合詳細ページを実際に1枚確認した結果、**設計の前提と異なる配信形式**が判明したため反映した。
事前に `robots.txt` を取得して 5.1 の記述と照合しており、一般クローラーへの制限がないことも再確認している。

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **`parser/` の責務を「HTML の解釈」から「レスポンス本文の解釈」に変更。** `extract.py`（埋め込みJSONの取り出し）と `fields.py`（フィールド対応表）を追加 | ボックススコアは HTML の表としてレンダリングされておらず、`<script>` 内の埋め込み JSON で配信されている。列見出しは JS のテンプレート文字列の中にあり、DOM を辿る実装では取れない。データ自体は初回レスポンスに入っているため、`httpx` で1回取れば足りる。`scraper/` と取得頻度・法務の立論は変更なし |
| 2 | **フィールド対応表を詳細設計 4.4 に明記。** `fd ← FOULON`、`fg2m/fg2a ← PT2M/PT2A`、`minutes ← PlayTime`（`"MM:SS"`）、`attendance ← Attendance` ほか20列 | 設計が要求する列すべてに対応フィールドが存在することを実地で確認した。恒等式（`Point = PT2M×2 + PT3M×3 + FTM`）の検算も通った |
| 3 | **取り込み前の正規化3点を規約化。** ①`PeriodCategory=18`（試合通算）で絞る ②`PlayerID` が空のチーム集計行を分離する ③空文字と 0 を区別する | どれも放置すると静かに壊れる。①を怠ると行数が7倍、②を怠ると両スタッツテーブルが汚染、③は `int("")` が例外になるか「記録なし」が「0回」になる |
| 4 | **取得しないフィールドを要件 5.3 に明記**（審判名・プレイバイプレイ700件規模・シュートチャート座標・導出値） | 同じレスポンスに同梱されている。「取得は必要最小限」（5.2）と「予測に不要な属性は除外」（5.3）に従い、対応表にある列だけを抜き出す |
| 5 | **fixture と `.gitignore` の対象を埋め込みJSONにも広げた**（`**/fixtures/**/*.raw.json`） | 取得した本文を生のままコミットしない、という 4.5.2 の趣旨は形式によらない |
| 6 | **`parser_canary` の検査内容を具体化**（JSONが取り出せる／必須フィールドが揃う／`PeriodCategory` に 18 がある） | HTML の構造だけを見ても、JSON 配信が壊れたことに気づけない |
| 7 | P0-1（入場者数）・P0-2（ポゼッション項目）・P0-9（FD と 2FGM/2FGA）を**取得可として記録** | いずれも埋め込みJSONに存在。動員系特徴量とポゼッション系特徴量を予定どおり実装できる |

## v1.13 での変更（工程4b）

残りの `/internal/*` を実装した。設計の変更は2点だけで、どちらも実装して初めて分かったことである。

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **freeze の Cron Trigger（毎時）を工程4b の範囲に明記した** | 基本設計 4.1 が「`finalize` は Workers の Cron Trigger」と定めていたが、実装順序の表に置き場所が書かれていなかった。`wrangler.toml` の `[triggers]` と `scheduled` ハンドラはここで入る |
| 2 | **`/internal/metrics/active` を `/internal/models` とは別のルータにすることを明記した** | 同じルータを2箇所にマウントすると、`/internal/metrics/active` がモデル一覧の `/active` に当たる。実際にテストが `{models: [...]}` を返して落ちた |

### 実装して分かったこと（テストの作り方に影響する）

**凍結済みの予測はテストの後片付けでも消せない。** `is_final = 1` の行は DELETE をトリガが拒否する。これは設計どおりの挙動であり、テストの都合で曲げない。代わりに**各テストが一意なIDを使い**、残骸と干渉しないようにした（`helpers.seedGame` が呼び出しごとに連番のIDを作る）。`resetAll` は「消せるものだけ消す」形にしてある。

**テストファイル間で D1 が共有される。** このプールのバージョンには isolated storage の指定がないため、並行実行すると片方の DELETE がもう片方の挿入を消す。`fileParallelism: false` で直列に走らせる。

**`predictions.model_version` は `model_versions(version)` を参照する。** モデル行を用意しない下地では FK 違反で 500 になる。テストの `seedGame` がモデル行も作る。

## v1.12 での変更（工程4a と工程2の D1 投入）

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **工程4を 4a / 4b に分けた。** 4a は「API の土台と `POST /internal/masters`」、4b は残りの `/internal/*` | 工程2の完了条件が「D1 への投入は工程4の後」となっていたため、工程2を閉じるには API の一部が必要だった。全部を1度に作ると量が大きく、masters だけで工程2が閉じる |
| 2 | **工程2の完了条件を「D1 投入まで完了」に更新した** | ローカル D1 に 11 / 30 / 30 行が入り、公式ID30件すべてが `club_id` に解決し、2回流しても行数が増えないことを実機で確認した |
| 3 | **api のテストは Workers ランタイム（`@cloudflare/vitest-pool-workers`）で走らせることを明記した** | `crypto.subtle.timingSafeEqual` と D1 の `batch()` は Workers の API であり、Node 上では検証できない。スキーマは `readD1Migrations('../db/migrations')` で読み、**テスト側に DDL を書き写さない** |

**`compatibility_date` は workerd の対応範囲を超えて書けない。** `2026-09-21` を指定するとローカル実行が起動しなかった（`This Worker requires compatibility date "2026-09-21", but the newest date supported by this server binary is "2026-08-22"`）。wrangler を上げたときだけ前に進める。

**このプールのバージョンには isolated storage の指定がない。** D1 の中身がテストを跨いで残るため、`beforeEach` で明示的に消す必要がある（実際に 0 期待で 1、30 期待で 31 になった）。

**CI の Node のバージョンが低すぎて api ジョブが落ちた。** 要件の「Node 20.9+」は Next.js 16 の下限であり、開発ツールチェーンの下限はこれより高い（`wrangler` が `>=22`、`eslint` が `^20.19 || ^22.13 || >=24`）。手元が Node 24 だったためローカルでは通り、**CI だけが落ちた**。手元と CI をそろえて 24 を使うことにし、`api/package.json` に `engines.node: ">=22"` を入れて早い段階で落ちるようにした。

**`wrangler types` の生成物はコミットしない。** `.dev.vars` を読んで型に含めるため環境依存になり、`git diff --exit-code` による検査が CI で必ず落ちる。`npm run typecheck` が `wrangler types && tsc --noEmit` を行い、以降のステップもその型を使う。

**`test_batch_limits_match_schema` が DDL と定数の食い違いを1件報告したが、原因はテスト側だった。** `-- 世代通番 1,2,3...` というコメント内のカンマで列が分割され、`predictions` が19列ではなく20列に見えていた。**コメントを落としてから分割する**ように直した。定数と設計文書の19は正しい。

## v1.11 での変更（工程3の CI）

工程3（CI 構築）に着手したところ、**完了条件の「`eslint .` が動く」を満たせない**ことが分かった。工程3の時点でリントする TypeScript が存在しない（`api/` は工程4、`web/` は工程11）。

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **ESLint Flat Config の設置を工程4へ移した。** 工程3の完了条件は「`ruff` / `mypy` / `pytest` と fixtures 検査が push で走り、ワークフローの不変条件がテストで固定されていること」に変えた | リポジトリ内の JS は `verification/` の計測スクリプト3本だけで、これをリントするために3つめの npm ルート（ルート `package.json`）を作るのは永続的な複雑さになる。**ESLint は最初の TypeScript と一緒に置く**のが素直である |
| 2 | **`api` / `web` のジョブは工程3で書いておき、`detect` ジョブの出力で分岐させる** | ディレクトリができた時点で自動的に有効になるため、後から CI に足し忘れることがない。`hashFiles()` を job 単位の `if` に書く方法は**使えない** — checkout より前に評価されるためワークスペースが空で、常に偽になる |
| 3 | **Dependabot の npm は後追いにした。** 工程3では pip と github-actions のみ登録する | 存在しないディレクトリを指定すると Dependabot がエラーを出し続ける |
| 4 | **`parser-canary` は `batch/parser` が無い間スキップする**（工程5で有効になる） | 毎日失敗させると通知が形骸化し、本当に壊れたときに気づけなくなる。`PARTIAL` を exit 0 で終えないという方針と同じ理由である |

**ワークフローの不変条件をテストで固定した**（`batch/tests/test_workflows.py`）。`permissions: contents: read` の存在、`contents: write` が `daily-ingest` / `gameday-update` 以外に無いこと、`concurrency` と `timeout-minutes` の存在、`next lint` の不在（コメントでの言及は許す）、サードパーティ Action の SHA ピン留め、cron が UTC であること。**YAML パーサは入れていない** — 見ているのは行と語の有無だけで、CI 設定のためだけに依存を増やす理由がない。

**fixtures 検査を実装した**（`scripts/check_fixtures.py`、受け入れ基準 A-12）。追跡ファイルに生のレスポンス本文（`.html` / `.raw.json`）が無いこと、fixtures に実在クラブ名（`clubs.csv` の30件を語彙にする）と抽出対象外のフィールド（審判名・プレイバイプレイ）が無いことを検査する。**意図的に違反を作って3件すべて検出し exit 1 になることを確認した。**

## v1.10 での変更（工程2の前提）

工程2（マスタ整備）に着手したところ、**`seed_master` が書くデータそのものに未定義の項目が3件**あった。

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **`POST /internal/masters` を追加した。** 名前付き配列（`seasons` / `clubs` / `clubSourceIds`）で受け、配列ごとに Zod スキーマを持つ | `/internal/*` は15本あったが、**マスタを書く口が1つもなかった**。D1 への書き込みは Workers 経由に一本化されているため（絶対ルール3）、口がないと `seed_master` が実行できない。**テーブル名を引数に取る汎用エンドポイントにはしない** — 「任意のテーブルへ書ける口」は関門の意味を失わせる。71行すべてが1リクエスト（5文）に収まる |
| 2 | **`clubs.slug` は手で決めて CSV に持ち、改称でも変えない** | 形式（`^[a-z0-9-]{1,40}$`）と用途（`/teams/[slug]`）だけが決まっており、値の決め方が未定義だった。英語名（`TeamNameE`）からの機械導出は、**2016-17 の試合で空文字だった実例があり**全クラブで取れる保証がなく、改称のたびに URL が変わる。`clubs` は恒久エンティティなので slug は恒久な識別子に徹し、表示名は `club_seasons.name` が持つ |
| 3 | **`seasons.start_date` / `end_date` は日程一覧から試合日の最小・最大を1回だけ導出し、CSV に固定する** | 両列は NOT NULL で、**URL 空間の有限化**（範囲外の日付を D1 到達前に404）に使われる無料枠の防壁である。公式の年間スケジュールページは当季のみ・ガント描画で11シーズンの出典にならない（Phase 0 で確認）。試合投入後に求める経路は `games.season_id REFERENCES seasons(id)` で循環する |

2 と 3 はどちらも、v1.9 で会場の座標に採った「**1回だけ確定して CSV に固定し、実行時には取得しない**」という扱いに揃えた。

**工程2の完了条件を変えた。** CSV を in-memory SQLite に適用して `club_source_ids` の紐付きをテストで確認できることとし、**D1 への実投入は工程4（API）の後**に行う。実装順序では工程2が工程4より前にあり、書き込み口がない段階では実投入ができないためである。

## v1.9 での変更（会場マスタの出典）

会場マスタの出典を実地で確認した（`verification/RESULTS.md`）。**設計の前提が3つ外れていた。**

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **`venues.id` に公式の会場ID（`StadiumCD`）を使う。** `v_` プレフィックスの独自採番と会場名による名寄せをやめ、`venue_source_keys` の列を `source_name` → `source_code` に変えて `club_source_ids` と同じ「公式ID → 内部ID」の対応表にした | 詳細設計 1.1 は「公式 ID が存在しないエンティティ（**会場など**）」としていたが、**試合の埋め込みJSONに `StadiumCD` があり全シーズンに存在する**。2016-17 の試合でも `StadiumCD:3`（船橋アリーナ）が取れ、`/arena_detail/?ArenaCD=3` が引ける |
| 2 | **会場行は backfill（工程6）が試合データから作る。** `seed_master`（工程2）が投入するのは `seasons` / `clubs` / `club_source_ids` だけになった | **会場の集合は事前に列挙できない。** クラブは代替会場を多く使い毎年変わる。`/arena/`（アリーナ検索）の一覧も AJAX で初回HTMLに `ArenaCD` が出ない。`club_seasons` と同じ構造である |
| 3 | **座標は国土地理院で1回だけ解決して CSV に固定し、収容人数は手入力にした** | **公式サイトに収容人数がない**（試合JSON・`/arena_detail/`・`/arena/` のいずれにもない）。座標もないが住所は取れるため、ジオコーディングで解決できる（実測: 豊橋市総合体育館 → 34.754406, 137.323517）。会場は一度確定すれば動かないため、日次バッチで外部サービスに依存する理由がない |

**収容人数は 2026-27 の26クラブのメイン会場から埋め、代替会場は NULL のまま進める。** NULL では `spectator_restricted` が NULL（判定不能）になるが、**観客制限期間は期間指定で強制的に 1** とするためコロナ期の Elo には影響しない。設計は既に NULL を許容している。

**「入場者数の最大値を収容人数とみなす」自動化は採らない。** 出典が不要で定義も正しく見えるが、**未来の試合から値を作ることになり絶対ルール1（データリークの禁止）に触れる**。観客制限期間では最大値そのものが抑制されており、判定が循環する。

**パーサの正規化が5つになった。** `StadiumCD` は 2016-17 が整数 `3`、2025-26 が文字列 `"169"` で型がシーズンで違う。正規化しないと同じ会場が2行に分裂する。

**未決事項 U-10 を追加した** — `venue_revisions` の名称履歴を自動生成するか手入力に持つか、衝突時の挙動、revision 行がない会場のフォールバック。工程6の着手前に決める。

## v1.8 での変更（Phase 0 / P0-5 の反映）

公式サイトのクラブ対応関係を実地で確認した結果（`verification/RESULTS.md`）を反映した。**`TeamID` が不変であることは設計どおりだったが、設計が想定していなかったものが3つ見つかった。**

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **`games` と `team_games` に `competition`（`REGULAR` / `PLAYOFF`）を追加**し、取り込み対象をリーグ戦とチャンピオンシップに限定した。オールスターゲーム・B1残留プレーオフ・B1・B2入替戦・アーリーカップは DB に入れない | 公式サイトの試合一覧が大会区分（`event`）で分かれており、**オールスターゲーム**が含まれていた。選抜チーム同士の対戦であり `clubs` に存在しないチームが現れるため、Elo に混入すると全チームのレーティングが歪む。入替戦は相手が B2 のクラブで対戦相手を解決できない。区分を列に残さないと特徴量 #17（順位・プレーオフ争いの状況）が算出できず、復元には backfill のやり直しが必要になる |
| 2 | **`club_seasons` を `seed_master`（工程2）から外し、backfill（工程6）が試合データから構築する**ことにした。`name` の出典はボックススコアの `TeamNameJ` | 年度別のクラブ一覧ページが存在しない（`/club/` に年度指定がなく、`/club_detail/` は初回レスポンスが空）。一方 `TeamNameJ` は**その試合の時点の名称**である（`703` は 2016-17 で `栃木ブレックス`、現在は宇都宮ブレックス）。`name` / `short_name` はともに NOT NULL であり、試合データが入る前には埋められない |
| 3 | **要件 6.6 の「B.ONE 時代の Elo があればリーグ間オフセットを推定して平行移動する」を削除**し、「前季にトップリーグの試合がないクラブは空白の長さを問わず昇格クラブ扱いとし、古い Elo を持ち越さない」に置き換えた | 取得対象はトップリーグのみ（要件 5.4）で下位リーグの Elo は**計算されないため、この分岐は永久に発火しない**。また **B.PREMIER の構成は昇降格の結果ではなく**、`718` 神戸（B1 在籍は 2017-18 のみ、以後8シーズン不在）のように古い Elo だけが残るケースが実在する。`SEASON_REGRESSION` を8回適用すると偏差が 3.2% に縮んで実質 1500（平均）になり、過大評価に倒れる |

**閾値を新設していない。** 「空白N シーズン以上なら復帰クラブ」という形にはせず、「前季に試合がなければ昇格クラブ扱い」の一本にした。11シーズンで空白を挟んだ復帰は7件（空白1年が4件、2年・5年・8年が各1件）しかなく、閾値を推定する検出力がない。

**バッチサイズが1テーブル変わった。** `games` は 23→24列だが `floor(100/24)=4` で上限160のまま、`team_games` は 9→10列で**上限が440→400**になった（詳細設計 3.4）。

`competition` は `db/migrations/0002_facts.sql` に直接追加した。**初期スキーマをまだ一度も適用していないため**この扱いにしている。一度 `migrations apply` を通した後は、追記のみ（新しい番号のファイル）に戻る。

## v1.7 での変更（凍結トリガの網羅）

工程1（マイグレーション）のテストを書いている最中に、**凍結が1テーブルだけ効かない**ことが分かったため直した。

| # | 変更 | 根拠 |
|---|---|---|
| 1 | **`player_predictions` に親参照の凍結トリガを追加**（`trg_ppred_parent_final_immutable` / `_nodelete`）。子4テーブルすべてが親の確定で守られる状態にした | `player_predictions` は自身の `is_final` しか見ていなかったため、**親が確定済みでも子の `is_final` が 0 なら UPDATE / DELETE が通った**（in-memory SQLite で実測）。DBトリガは「アプリ層の規律だけに頼らない」ための関門であり、`finalize` が子への UPDATE を取りこぼすと無効化される状態では関門として機能していない |
| 2 | **freeze の「子 → 親」の順序を「必須」に格上げ**し、根拠の説明を訂正 | v1.5〜v1.6 の記述は「親を先に確定させると子行への書き込みが親参照トリガに拒否される」としていたが、これは親参照トリガを持つ3テーブルには当てはまり、**freeze が実際に UPDATE する `player_predictions` には当てはまらなかった**。#1 により順序が実際に load-bearing になった（親を先に立てると freeze 自体が失敗する） |
| 3 | 凍結のテストを網羅形に整理（`test_children_frozen_when_parent_final` / `test_parent_referencing_children_frozen_by_parent_alone` / `test_freeze_fails_if_parent_frozen_first`） | 旧テスト名はテーブルごとに分かれており、子を1つ足したときに取りこぼす形だった |

トリガは `db/migrations/0008_freeze_triggers.sql` に追記した。**初期スキーマをまだ一度も適用していないため**この扱いにしている。一度 `migrations apply` を通した後は、追記のみ（新しい番号のファイル）に戻る。

