# 現在地

**最終更新: 2026-09-26（止まっていた判断を全部確定させた。U-09 解決、ホーム勝率の前提を実測へ）**

このファイルには、**リポジトリと設計文書からは分からないことだけ**を書く。

| 書くもの | 書かないもの |
|---|---|
| 進行中の作業と、何を待っているか | 工程の完了状況 → `design-detail.md` 9章 |
| 運営者の判断待ちの項目 | 設計判断とその理由 → 各設計文書 |
| 本番の運用状態（取り込み済み、設定済み） | 変更履歴 → `git log` |
| 次の一手 | 測定結果 → `verification/RESULTS.md` |

二重に書くと必ず片方が古くなる。**ここは短く保つ。**

---

## いま何をしているか

| 作業 | 状態 |
|---|---|
| **過去データの取り込み（工程6）** | **2016-17 / 2017-18 / 2018-19 が完了。2019-20 が 210試合まで。**次は 2019-20 の残り（約170試合）から（AI が `gh workflow run` で起動） |
| 依存の更新 | **開いていた4件を見送って閉じた**（`typescript-eslint` が TS 7 を許さない、`@cloudflare/vitest-pool-workers` が vitest 5 を許さない、pytest は既に新しい）。peer が追随したら上げる |
| 公開API（工程10） | **10a・10b 完了。** `/health` 以外の6本が応答する（api のテスト 239件）。`/health` の `quota` だけ下記の判断待ち |
| 会場の座標（工程6） | 経路は全部できた（`GET /internal/venues` → `resolve_venue_geo`）。**実行は取り込み完了後に1回** |
| **工程8（学習）** | **コードを書いて実データで通した**（指標・ベースライン3段・walk-forward・LightGBM）。**採用判定はしていない** — 下記の判断待ち2件を先に決める。数値は `verification/RESULTS.md` |
| 静的JSON の全体像（U-09） | **解決した。** 詳細設計 3.7 に確定（案B）。工程9と11bの前提が開いた |
| **静的JSON の書き出し（工程9a）** | **完了。** `batch/static_json/` と `contracts/public-shapes.json`（キー構造の契約。api と batch の両方が読む）。**残りは 9b（推論との結線）で、工程8の採用判定を待つ** |

## 決まったこと（再検討しない）

| # | 決定 | 経緯 |
|---|---|---|
| **UI のレイアウト** | **現状維持。** 基本設計 5.2 の「大きいカード1枚 ＋ 圧縮カード」で進める | 2026-09-26 に5案（現状 / リスト均等 / バー主役 / 根拠つき / 確信度グループ）を実機で比較し、運営者が現状を選んだ。**同じ検討を繰り返さない**。比較したモックは Artifact に残してある |
| トップで大きく出す試合 | **その日の最初の試合**（開始時刻の昇順の先頭） | 「次に始まる試合」にしない — 静的配信では「いま」を知らず、時刻で変わる表示はキャッシュと噛み合わない |
| **U-09 静的JSON** | **案B。** 一覧は当日＋7日、**詳細は当日だけ**。スキーマは公開APIと同一 | 詳細設計 3.7。判断の軸は public リポジトリの履歴に積む量（7日窓の詳細は年460MB、当日だけなら年90MB） |
| **`HOME_ADVANTAGE` のグリッド** | **`{0, 10, 20, 30, 45}` / 初期20** | 実測のホーム勝率 52.7% に対応するのは 18.8点。旧グリッド `{40,55,70,85}` は**実測値を1つも含んでいなかった**（要件 1.13 / 詳細設計 2.5） |
| **Elo パラメータ探索** | **2段に分ける。** 段1は Elo単体ロジスティック回帰の Brier、段2はモデル側 | 素朴に回すと 300通り × 21分 = 105時間で 120分枠に入らない（詳細設計 2.5） |
| **404 の文言** | **「お探しのページはありません。」** | 要件 8.5 の空状態の表に追加した（1.13）。要件の表になかったのは漏れであり、出さない意図ではない |
| **E2E（Playwright）** | **工程15で入れる。** いま入れない | A-06（テーマの保持）/ A-07（免責への到達）は品質ゲートであり**必須**だが、工程15が「A-01〜A-18 をすべて満たす」段である。A-07 の一部は `npm run test:links` が既に守っている。Playwright はブラウザ込みで重く、画面が固まる前に入れると維持費だけ先に発生する |

## 運営者の判断待ち

| # | 事項 | 誰が決めるか | 影響 |
|---|---|---|---|
| — | **`/health` の `quota` の出所。** Workers リクエスト数とキャッシュヒット率を数える先がない（KV も Durable Objects も従量課金で絶対ルール5に触れる） | 運営者 | 受け入れ基準 A-10 の検証手段 |
| — | **短い試合（CS の決着戦）を Elo でどう扱うか。** 10分規模の得点差を40分の試合と同じ `multiplier` で扱ってよいか | 工程8で決める | Elo のパラメータ探索 |
| — | **Elo の σ が 191 で他リーグ（100〜150）より広い。** `K` か得点差の `multiplier` が強い可能性 | 工程8の探索で決める（先に直さない） | Elo のパラメータ |

## 運営者にしかできない作業

| いつ | 内容 |
|---|---|
| **規約 / robots が変わったとき** | **本文を読んで承認の可否を判断する。** AI は調査までで、承認はしない（絶対ルール6・法務） |
| backfill 後 | 会場の**収容人数**の確認。**調べるのは AI** — 公式サイトに無いため外部の情報源を当たり、出典URLつきで CSV を埋める。運営者が見るのは**情報源が食い違う行と、建物の最大収容しか見つからない行**だけ（定義は「B.LEAGUE 開催時の観客席数」に固定されており、そこは判断が要る） |
| Phase 1 の前 | 正式名称の確定と商標調査（U-01 / P0-8） |
| Phase 1 の前 | 法律相談（要件 4.5.8 の論点1〜3） |
| Phase 1 の前 | B.LEAGUE への事前連絡（要件 4.5.6） |
| Phase 1 の前 | 独自ドメインの取得（WAF レートリミットに必要） |

## backfill は AI が実行する（2026-09-25 から）

運営者の指示により、`gh workflow run` で AI が起動する。**守ること。**

- **1日の書き込み見積りを 92,000行に収める**（枠は10万行で 00:00 UTC にリセット）。
  シーズン単位ではなく行数で管理する — 「1日1シーズン」だと毎日34%を捨てる
  - 1試合 ≈ **118.6行**、1シーズン ≈ **65,800行**（実測）
  - 当日の消費 = 当日UTC の `ingestion_logs`（`job='backfill'`）の `rows_affected` 合計 × 118.6
  - 端数のシーズンは `-f limit=floor((92000 − 消費 − 200) / 118.6)` で起動する
  - **1日の起動は最大2回**（1シーズン ＋ 端数）。3回以上に分けると日程29ページの取り直しが増える。
    ただし**取りこぼしを拾う再実行（数試合）はこの数に入れない**（日程29ページ＋数件で済む）
- **実行前に宣言し、枠と本日の実行履歴を確認する**
- **規約 / robots が変わったら絶対に承認せず、止めて報告する。** 承認は運営者のみ
- ログは `gh run view <id> --log` で AI が読む（スクリーンショットは不要）

`gh` は `yozamaru` として認証済み（スコープ `repo`）。Variables の読み書きもできる。
**Secrets は読めない**（設計どおり）。

## 本番の状態

リポジトリからは分からないため、変えたら必ずここを直す。

| 対象 | 状態 |
|---|---|
| D1 `bpredict` | `6af3adc9-3f7f-4b10-b374-efde48e1339a` / APAC(NRT) / マイグレーション `0010` まで適用済み |
| D1 のデータ | マスタ ＋ **2016-17 / 2017-18 の試合**（`games` 1,109）。スタッツ欠けは0件 |
| Worker | **`https://bpredict-api.naomaru.workers.dev`**（2026-09-26 に workers.dev のサブドメインを `bpredict` → `naomaru` へ変更）。毎時の Cron Trigger（freeze）が稼働 |
| Worker のデプロイ版 | **2026-09-26 に更新**（Version `f3831f6b`）。公開API（`/games/:gameId` / `/accuracy`）を含む。**「デプロイ済み」だけ書くとコードとの差が見えない** — 09-22 版のまま公開APIが404を返していたことに、URL変更の確認で初めて気づいた。**次に上げたらこの行を更新する** |
| Workers Secret | `INGEST_TOKEN` / `FINALIZE_TOKEN` 設定済み |
| GitHub Secret | `INGEST_TOKEN` 設定済み |
| GitHub Variables | `API_BASE_URL` / `SCRAPER_USER_AGENT` / `SCRAPER_ROBOTS_SHA256` / `SCRAPER_TERMS_SHA256` 設定済み |
| 稼働しているワークフロー | `ci` / `parser-canary`（日次）/ `backfill`（手動）。**日次・当日・月次は未実装** |
| 公開APIの応答 | `/accuracy` は 200 で空の集計（**予測が0件**。工程9が未着手）。`/games/:gameId` は404、不正なIDは400 |

### 取り込み済みシーズン

| シーズン | 状態 |
|---|---|
| 2016-17-B1 | **完了**（555試合 = リーグ戦540 + CS 15。`不正=0` / `非リーグ戦=2`） |
| 2017-18-B1 | **完了**（554試合 = リーグ戦539 + CS 15。`不正=0` / `非リーグ戦=1` / **`未実施=1`**） |
| 2018-19-B1 | **完了**（555試合。1回目は `不正=11`＝公式サイトの **502** で、再実行で11試合を拾った） |
| 2019-20-B1 | **210 / 約380**（**COVID で中断したシーズン**。`状態不明=173` は `試合中止` の行で、実地確認した） |
| 2020-21〜2026-27 | 未着手 |

**2017-18 は1試合（`1810`）が入っていない。** サイト側にボックススコアが存在せず
（試合ページはあるが埋め込みJSONの代入自体がない）、取得しようがない。
リーグ戦が540ではなく539になっているのはこのため。**下記の「観察中」を参照。**

**本日（2026-09-25 UTC）の枠は使い切った** — 765試合 ＋ seed_master 5回 ≈ 91,700行 / 92,000。
次は **09:00 JST（00:00 UTC の枠リセット）以降**に、2019-20 の残りから流す。

## 会場の緯度経度は AI の作業（2026-09-25 に整理を訂正）

**運営者の作業として挙げていたのは誤りだった。** 設計（詳細設計 1.2）は最初から
自動の経路を定めている。2026-09-25 に両端を実地確認した。

| 経路 | 確認結果 |
|---|---|
| `/arena_detail/?ArenaCD=<cd>` | **住所がある**（例: `住所 千葉県船橋市習志野台7-5-1`）。**収容人数は無い** |
| 国土地理院の住所検索 | 座標を返す（無料・APIキー不要。要出典表記）。上記の住所で `[140.052017, 35.729637]` |
| D1 への書き込み口 | **既にある** — `POST /internal/games` の `venues` 配列が `prefecture` / `lat` / `lng` を更新する |

**取り込みが全部終わってから1回だけ流す。** 会場は取り込みとともに増えており
（2026-09-25 時点で88件、全シーズンで120件前後の見込み）、途中で解決すると
同じことを2回やることになる。設計の「1回だけ解決して CSV に固定する」に従う。

**未解決の1点: 会場IDの一覧をどこから取るか。** 設計に書かれていない。
`venues` は D1 にあり、これは運用上の読み取りなので `/internal/*` の GET を1本
置くのが既存の型に合う（詳細設計 3.4）。**推測で決めずに、実装の前に確認する。**

## 工程8に持ち込む申し送り

**欠場系の3キーが定数0である。** `minutes_lost_diff` / `top_players_out_diff` /
`entry_is_official` は `game_entries` を使うが、この表は**0行**である（backfill は
エントリーを取得せず、取得するのは未実装の `gameday_update`）。

**採用ゲートの「欠損率30%超で不採用」はこれを捕まえない** — NULL ではなく定数0であり
`feature_null_rates` は 0% と出る。**工程8で σ=0 の列を検出する検査を足し、設計
（詳細設計 4.6）にも反映する。**

**学習は「コードを通す」までを先に、採用判定とパラメータ探索は全シーズン後に。**
4シーズンでは fold が2つしかなく、最初のシーズンは Elo が 1500 から始まり、
2019-20 は COVID で中断している（`verification/RESULTS.md`）。

## 観察中（判断を保留しているもの）

**ボックススコアが未公開の試合をどう扱うか。** 2017-18 の `1810` で初めて出た。
日程にはスコアが載っているため、**日程の情報だけで `games` と `team_games` を作れば
Elo には結果を渡せる**。しかし1試合ぶんの損失は小さく（540試合中1件）、偏りもない。

**件数が溜まるまで判断しない。** 2016-17 は0件、2017-18 は1件。
11シーズンで十数件に収まるなら現状（スキップして報告）で足りる。
どれかのシーズンで大量に出たら、日程からの取り込みを設計に足すか判断する。

| シーズン | 未公開の件数 |
|---|---|
| 2016-17 | 0 |
| 2017-18 | **1**（`1810`） |

## 気をつけること（実データで踏んだもの）

`verification/RESULTS.md` に詳細がある。**同じ罠を二度踏まないために、ここには見出しだけ置く。**

- 内部APIへのリクエストは **User-Agent を送る**（urllib の既定は Cloudflare が 403 で弾く）
- **1シーズンの D1 書き込みは約65,800行 / 1試合 118.6行**（設計の見積り26,500行は外れていた）。上限はシーズン数ではなく行数（詳細設計 4.8）
- 公式サイトから 429/503 を受けたら、クライアントがそのUTC日の取得を自動停止する。**朝（09:00 JST）まで待つ**
- D1 は `PRAGMA foreign_keys=OFF` を無視する。**データが入ったままでは参照されているテーブルを作り直せない**
- **workers.dev のサブドメインを変えると旧URLは NXDOMAIN になる**（リダイレクトされない）。
  `API_BASE_URL` の Variable と `docs/STATUS.md` を同時に直す。新URLの TLS 証明書は
  変更直後は出ておらず、`sslv3 alert handshake failure` になる（数分〜数十分で解消する）
- **日程の行は全部が `<a>` ではない。** 中止・延期の行はリンクを持たず、状態欄が
  `<script>` だけの行もある（2018-19 の CS）。1件で**シーズン全体が落ちた**。詳細設計 4.4
- 公式サイトは**たまに 502 を返す**（`ResponseError`）。2018-19 では555試合中11件。**1試合の失敗ではジョブを止めず、飛ばして続ける**（連続3件で中止）。再実行で拾う
