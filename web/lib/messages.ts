// ユーザーに見える日本語は**設計の一部**である（CLAUDE.md「勝手な仕様補完をしない」）。
// 散らばると必ずぶれるため、空状態の文言はここだけに置く。
//
// **出典は要件 8.5 の表であり、文言をここで変えない。** 詳細設計 5.3 の同じ表は
// 句点を省いた短縮形で書かれているが、要件が上位文書である（CLAUDE.md の優先順位）。

/** 試合がない日。**次の試合日へのリンクを添える**（要件 8.5） */
export function noGames(dateLabel: string): string {
  return `${dateLabel}に予定されている試合はありません。`;
}

/**
 * オフシーズン。**的中率ページへの導線を添える**（要件 8.5）。
 *
 * Bリーグは年間の1/3以上がオフシーズンであり、空状態の設計は例外処理ではなく
 * 主要画面の一つである。
 */
export const OFF_SEASON = '今シーズンの全日程が終了しました。';

/** 予測がまだ生成されていない試合（要件 8.5）。導線は付けない */
export const NO_PREDICTION = 'この試合の予測はまだありません。試合前日の朝に公開されます。';

/** 取得に失敗したとき。**`/about` へのリンクを添える**（要件 8.5） */
export const LOAD_ERROR = '予測を読み込めませんでした。時間をおいて再度お試しください。';

/** 空状態に添える導線のラベル。文言と同じ理由でここに置く */
export const ACTIONS = {
  accuracy: { href: '/accuracy/', label: '的中率を見る' },
  about: { href: '/about/', label: 'このサイトについて' },
} as const;
